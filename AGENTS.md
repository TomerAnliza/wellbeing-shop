# ברוכים הבאים — wellbeing-app

זהו מסמך הכניסה המשותף לכל סוכן שעובד כאן — Claude, Codex או כל סוכן אחר.
**קרא אותו לפני כל פעולה.**

## הדבר הראשון: Git

מ-7 בספטמבר 2026 הפרויקט מנוהל ב-Git ומסונכרן ל-GitHub. **כל עבודה
מתחילה ב-`git pull` ומסתיימת בקומיט ו-push.** עבודה שלא נדחפה לא קיימת
עבור שאר הצוות.

| פרט | ערך |
|---|---|
| ריפו | https://github.com/TomerAnliza/wellbeing-app (ציבורי) |
| חשבון GitHub | `TomerAnliza`, החשבון של tomer@analiza-college.co.il |
| ענף ראשי | `main` |
| פרוטוקול | HTTPS דרך `gh` (המחשב הזה כבר מחובר; `gh auth status` לבדיקה) |
| זהות לקומיטים | `git config user.name "Tomer"` ו-`git config user.email "tomer@analiza-college.co.il"` בתוך הריפו |

אין טוקנים או סיסמאות במסמכים. ההרשאה נשמרת ב-keyring של `gh` במחשב.
סוכן שמריץ ממחשב אחר עושה `gh auth login` פעם אחת בחשבון הזה.

נוהל העבודה המלא: [docs/git-workflow.md](docs/git-workflow.md).

## מה הפרויקט הזה

**אפליקציית מעקב אחרי פעילות ספורטיבית.** משתמש מתחיל אימון בלחיצה,
רואה אותו רץ עם טיימר, מרחק וקלוריות, שומר אותו בסיום, ורואה את
ההתקדמות השבועית שלו מול יעד.

- **העיצוב הוא מקור האמת.** [`design/`](design/), כפי שמתועד ב-[docs/design-system.md](docs/design-system.md).
  סתירה בין מסמך לבין העיצוב — **העיצוב גובר**, והסתירה מדווחת.
- **אפיון המסכים:** [docs/spec-mvp-screens.md](docs/spec-mvp-screens.md). נגזר מהעיצוב, לא קודם לו.
- **הסטאק המומלץ:** [docs/tech-stack.md](docs/tech-stack.md). טרם אושר סופית.

זהו פרויקט **משותף**: כמה סוכני AI ובני אדם עובדים בו יחד, במקביל.
אף אחד לא רואה את השיחות של האחרים — לכן **התיעוד הוא הזיכרון המשותף**,
והוא חלק מהתוצר, לא תוספת לו.

## שני הכללים המרכזיים

1. **עיצוב לפני מסמכים, ומסמכים לפני קוד.** הפרויקט עובד **design-first**:
   קודם מעצבים את המסך, ממנו נגזר המסמך, וממנו נכתב הקוד. מסך שאינו
   מעוצב — לא מאופיין ולא ממומש. קוד בלי מסמך שקדם לו — לא נכתב.

2. **כל הקוד מתועד במסמכים ברורים לבני אדם.** מי שלא היה בשיחה — אדם או
   סוכן — צריך להבין מהמסמכים לבד מה נבנה, למה, ואיך להמשיך מאותה נקודה.

## כללי עבודה

- **שפה:** עברית בתיעוד ובהסברים. שמות קבצים, קוד ומזהים באנגלית.
- **תאריכים מוחלטים תמיד** — "7 בספטמבר 2026", לא "היום".
- **אל תמחק תיעוד קיים.** מסמך שגוי מתוקן במסמך חדש שמפנה אליו.
  איפוס של תיעוד נעשה רק בהוראה מפורשת של תומר.
- **אל תניח הקשר קודם** — כתוב למי שלא היה כאן.
- **עיצוב:** תומר מכין את העיצוב ומוסר אותו לצוות. סוכנים אינם מייצרים
  כיווני עיצוב מיוזמתם. סוכן שמזהה מסך חסר בעיצוב — **מדווח ועוצר**,
  ואינו ממציא אותו. רשימת החסרים: "מה חסר בעיצוב" באפיון.
- **תחום המוצר:** מעקב פעילות ספורטיבית **בלבד**. רעיונות למוצרים אחרים
  אינם נכנסים ל-`docs/`.

## הצוות

| סוכן | תפקיד |
|---|---|
| **צחי** (`tzachi`) | הסוכן האדוורסרי — חוקר כל רעיון ומחווה דעה נגדית מנומקת, כדי לתקף הנחות לפני שבונים עליהן. זמן אותו לפני כל החלטה משמעותית. מוגדר ב-`.claude/agents/tzachi.md` |

## סדר עבודה מומלץ

1. `git pull` — ואז קרא את [docs/design-system.md](docs/design-system.md)
   ואת [docs/spec-mvp-screens.md](docs/spec-mvp-screens.md) במלואם,
   ופתח את הפרוטוטייפ ב-`design/` בדפדפן.
2. לפני החלטה משמעותית — ארכיטקטורה, סכמה, תוכנית מימוש — זמן את צחי.
3. תעד את ההחלטה ב-`docs/journal/` (רשומה עם תאריך מוחלט, מה נעשה ולמה).
4. רק אז קוד.
5. בסוף כל משימה: קומיט עם הודעה ברורה ו-`git push`.

## מבנה

```text
wellbeing-app/
├── .gitignore         # קובצי .env לעולם לא נכנסים לריפו
├── AGENTS.md          # הקובץ הזה — כניסה משותפת לכל הסוכנים
├── CLAUDE.md          # מפנה לכאן (נטען אוטומטית לסוכני Claude)
├── README.md          # שער לבני אדם
├── .claude/agents/    # הגדרות סוכנים של הפרויקט (צחי)
├── design/            # העיצוב — מקור האמת. פרוטוטייפ React שמפרק את עצמו
├── ideas/             # ריק. רעיונות מחוץ לתחום המוצר אינם נשמרים כאן
└── docs/
    ├── design-system.md                # טוקנים שחולצו מהעיצוב
    ├── spec-mvp-screens.md             # אפיון המסכים — נגזר מהעיצוב
    ├── tech-stack.md                   # הסטאק המומלץ
    ├── git-workflow.md                 # נוהל Git — קומיט, push, ענפים
    ├── web-app-setup.md                # הרצה ודיפלוי של web/
    └── journal/                        # יומן החלטות, רשומה לכל שינוי משמעותי
```
