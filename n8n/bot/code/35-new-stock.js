// ── מלאי חדש ─────────────────────────────────────────────────
// הזמנה מורידה מלאי מיד, בלשונית products.
// ביטול הזמנה אינו מחזיר מלאי אוטומטית — הנציג מעדכן בגיליון.

const order = $('שורת הזמנה').first().json;

return [{ json: { id: order._pid, stock: Math.max(0, order._newStock) } }];
